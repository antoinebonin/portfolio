<section class="hero">
    <div class="left">
        <p class="title">{{$title}}</p>
        <p class="subtitle">{{$subtitle}}</p>
        <p class="intro">{{$intro}}</p>
    </div>
    <div class="img">
        <img src="{{$image}}" alt="{{$title}}">
    </div>
</section>
